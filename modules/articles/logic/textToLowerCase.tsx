/**
 * This function accepts a string as paramater and return a text transformed to lowercase letters
 * @param text - The function parameter
 * @returns Returns a text tranformed to uppercase
 */

const textToLowerCase = (text: string): string => {
  return text.toLowerCase();
};

export default textToLowerCase;
