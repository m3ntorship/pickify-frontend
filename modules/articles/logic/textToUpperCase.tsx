/**
 * This function accepts a string as paramater and return a text transformed to uppercase letters
 * @param text - The function parameter
 * @returns Returns a text tranformed to uppercase
 */

const textToUpperCase = (text: string): string => {
  return text.toUpperCase();
};

export default textToUpperCase;
