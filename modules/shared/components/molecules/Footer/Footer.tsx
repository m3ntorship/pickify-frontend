import React from 'react';
import type { FC, ReactElement } from 'react';
import type { IFooter } from './IFooter';

const Footer: FC<IFooter.IProps> = (): ReactElement => {
  return <div className="" />;
};

export default Footer;
