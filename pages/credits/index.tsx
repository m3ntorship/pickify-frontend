import React from 'react';
import type { FC, ReactElement } from 'react';
import { CreditsPage } from '@modules/CreditsPage/';

const Credits: FC = (): ReactElement => {
  return <CreditsPage />;
};

export default Credits;
